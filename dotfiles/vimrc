" Originally copied from Tim Monks

set nocompatible

if &term =~ "screen"
	set term=xterm
endif

set history=50
set autowrite
set backspace=2 "mean that you can always backspace

filetype plugin on
filetype on
