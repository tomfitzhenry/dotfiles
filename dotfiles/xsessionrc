gnome-settings-daemon & # Have to wait for this to finish
sleep 3

if [ "$(hostname)" = "tom-MS-7599" ]; then
    xrandr --output DFP2 --auto --right-of CRT1 --auto
elif [ "$(hostname)" = "macbook" ]; then
    xrandr --output DP-2 --off
fi

xmodmap ~/.xmodmaprc
ssh-agent &
wmname LG3D &
dropbox start &

emacs -daemon &
btsync &
dropbox start &

fluxgui &
trayer --edge top --align right --SetDockType true --SetPartialStrut true \
       --expand true --width 10 --transparent true --tint 0x191970 --height 15 --monitor 1 &
nm-applet &
