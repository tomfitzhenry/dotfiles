function create_temp_dir() {
    local ROOTTEMPDIR=~/tmp/
    mkdir -p $ROOTTEMPDIR

    local PREFIX=${1:-"tmp"}

    local TEMPDIR=$(TMPDIR=$ROOTTEMPDIR mktemp -d -t $PREFIX)

    pushd $TEMPDIR
}

alias t="create_temp_dir"
